<section id={{CVTypes.awards.id}} class=cv-section>

  {{> cv/header-section/index CVTypes.awards.header}}

  <ol>
    {{#each CVItems.awards}}
      <li id='{{cvid}}' class=cv-item>

        {{> cv/date/index}}

        <div class=cv-item-content>

          <p class=cv-item-title>
            {{#if links.info}}
              <a aria-label='Award Title: {{title}}' href='{{links.info}}'>{{title}}</a>
            {{else}}
              <strong>{{title}}</strong>
            {{/if}}
          </p>

          {{!-- This content is automatically wrapped in tags by the Markdown helper --}}
          {{md description}}

        </div>

      </li>
    {{/each}}
  </ol>

</section>
